package sample;

class Chains {
    void run() {
        var names = people.stream().filter(person -> person.age() > 18).map(Person::name).sorted().toList();
        var one = builder.build();
        var kept = source
                .trim()
                .toLowerCase();
    }
}
