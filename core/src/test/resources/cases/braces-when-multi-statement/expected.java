class Multi {

    void run(int n) {
        if (n > 0)
            log(n);
        if (n > 0) {
            log(n);
            log(n);
        }
        while (n > 0)
            n--;
        for (int i = 0; i < n; i++)
            log(i);
    }

}
