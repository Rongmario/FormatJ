class Preserved {

    void run() {
        call(
                alpha,
                beta
        );
        int total = one
                + two;
        if (ready) { go(); }
    }

    void thrower()
            throws OneException, TwoException { }

}
